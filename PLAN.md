# 第81回日本物理学会年次大会・登壇者所属調査 計画

## 1. 目的と対象

2026年9月14日～17日に開催される日本物理学会第81回年次大会について、公開された講演プログラムのHTMLから登壇者の所属機関を調べる。講演ごとの根拠を領域別CSVに残し、全体および領域別の所属機関上位20位を日本語のMarkdownにまとめる。

- 対象は公式WEBプログラムに掲載された口頭発表・ポスター発表の講演。一般、招待、企画、シンポジウム等を含む。取消・講演番号だけの空枠・休憩・座長・インフォーマルミーティング・展示は除外する。
- **集計単位は人の実人数ではなく講演**。同じ人が別の講演で登壇すれば、その都度数える。著者全員の所属ではなく、特定した登壇者本人の所属を使う。
- 本調査では「2026年度第81回」ではなく、公式表記に合わせて「第81回年次大会（2026年）」と記す。
- 大会前の取得結果は暫定版と明記し、会期後に再取得・再集計して最終版を作る。プログラムの訂正、取消、登壇者変更を反映した取得日時を記録する。

公式の入口：

- 大会WEBプログラム: https://onsite.gakkai-web.net/jps/jps_search/2026au/index.html
- 領域別プログラム（口頭・ポスターの一覧）: https://jps2026a.gakkai-web.net/data/html/program.html
- 検索画面（講演番号・登録番号などの照合に利用）: https://jps2026a.gakkai-web.net/data/search/index.html

公式の領域別ページには「○」付きの著者、取消表示、合同領域の講演が実際にある。HTMLの構造・表記は少数の口頭・ポスター・合同セッションで先に検証し、仮定が外れればパーサーを修正する。検索画面の一括PDF取得は使わず、公開プログラムのHTMLのみを `wget` で取得する。

## 2. 成果物とディレクトリ

```text
.
├── PLAN.md
├── README.md
├── .gitignore
├── data/
│   ├── source_urls.csv              # 取得対象URL・領域・口頭/ポスター
│   ├── affiliation_aliases.csv      # 略称→機関正式名称の確定済み対応表
│   ├── area_aliases.csv             # HTMLの領域名と正規化した領域名
│   └── raw/                         # wgetしたHTML、Git管理外
├── scripts/
│   ├── download.sh                  # HTML取得、一覧からURLをたどる
│   ├── parse_program.py             # 講演・著者・登壇者・所属の抽出
│   ├── normalize_affiliations.py    # 略称抽出・対応表の適用
│   └── build_report.py              # 全体・領域別の集計
├── output/
│   ├── areas/                       # 領域ごとのCSV（口頭とポスターを統合）
│   ├── unresolved_presenters.csv
│   ├── unresolved_affiliations.csv
│   ├── audit.csv                    # 除外・重複・要確認の記録
│   └── report.md                    # 最終報告書
└── tests/                            # 登壇者マーク、所属記号、合同講演の固定例
```

`data/raw/` のみを少なくとも `.gitignore` に登録し、取得済みHTMLをGit管理しない。公開リポジトリに講演ごとの氏名・題名入りCSVを置く場合は、公式サイトの利用条件と公開範囲を確認したうえで扱う。集計表には氏名を出さない。取得URL一覧、対応表、スクリプトはGit管理する。領域ファイル名は `area11.csv`、`computational_physics.csv`、`particle_theory.csv` 等の固定IDを使う。

## 3. 取得

1. `program.html` に列挙された口頭・ポスターの領域別リンクを収集し、`data/source_urls.csv` に `area_id,area_name,format,source_url,local_path` を記録する。領域横断企画も対象に含める。リンクに現れない講演については検索画面・日程別一覧と突き合わせ、漏れを調べる。
2. `wget` による取得は、事前検証用の少数サンプルを含め、必ずGit管理するシェルスクリプト（標準は `scripts/download.sh`）またはGNU Makefileのターゲット経由で実行する。取得URL、保存先、`wget` のオプション、取得間隔、再試行条件をスクリプト・MakefileまたはGit管理する設定ファイルに記録し、同じコマンドで取得手順を再現できるようにする。端末での単発の `wget` 実行だけで取得を済ませない。上記URLと対象の領域別HTMLは `data/raw/` に保存する。PDF、画像、サイト全体の再帰ミラーリングは行わない。取得間隔を設け、失敗時は件数・URLを記録し、欠落したまま集計を成功扱いにしない。
3. 取得日時（UTCまたはJSTを明記）、元URL、ファイルのSHA-256をマニフェストに残す。ローカルHTMLのみを入力にして再解析できるようにし、解析スクリプト内でネット検索しない。
4. 会期後の再取得では古い版を上書きする前にマニフェストと件数を保存し、取消や登壇者の変更点を確認する。

## 4. 講演・登壇者の抽出

- HTMLの要素構造を使って講演を抽出し、講演番号、登録番号（存在する場合）、題名、掲載領域、発表形式、著者リスト、所属リスト、各上付き所属記号、登壇者記号を保持する。文字列全体を単純なカンマ分割だけで処理しない。
- 同一講演が合同領域の複数ページに載る場合は、公式の講演番号または登録番号で重複を検出し**全体では1回だけ**数える。領域別も「先頭掲載領域」を主領域として1領域にだけ割り当てる。主領域が判定できない場合は `audit.csv` に保留し、二重計上を避ける。合同先の領域名はCSVに残す。
- 登壇者は著者に付いた「○」等の明示的な表示を最優先する（`explicit`）。明示表示がなく、登壇者の別記もない場合のみ筆頭著者を暫定的に推定する（`inferred_first_author`）。筆頭著者規則は**推定**であり、検証済みと表示しない。名前の揺れで位置がずれる、複数の登壇者マークがある、著者欄自体が読めない等は `unresolved_presenters.csv` に出す。著者が1人なら `single_author` とする。
- 登壇者の所属は著者の上付き記号と所属一覧の記号を対応付ける。記号のない著者は記号なしの所属に対応させる。登壇者に複数の所属が明示されていれば全て保持し、同じ講演・同じ正式機関の重複表記は1件に畳む。対応が曖昧なら所属を推測して確定させず、未解決として残す。
- 取消講演は除外し `audit.csv` に理由と講演番号を記録する。ポスターの番号・構造、移動・変更表示も個別に検証する。

## 5. 所属機関の正規化

各登壇者について `元の所属表記 → 機関略称 → 正式機関名` を別々に保持する。例：`慶大物理 → 慶大 → 慶應義塾大学`（公式表記を採用）。`東大理`、`東大院理`、`東大物性研` はいずれも `東京大学` に統合するが、部局名を含む元の表記は失わない。大学以外の国立研究機関・企業・海外機関なども、同じ機関単位で表記を正規化する。共同利用機関と大学、独立法人とその連携部局など、法人が異なる場合は安易に統合しない。

`data/affiliation_aliases.csv` は最低限 `alias,official_name,source_url,note` とし、同一略称に複数の機関があり得るときは機械的に一意決定しない。未登録・曖昧な略称はまず公式機関サイト等で検索して正式名称を確認し、根拠URLとともに対応表へ追記する。それでも決まらない場合は `output/unresolved_affiliations.csv` に `raw_affiliation,alias,count,example_presentation_id,reason` を出す。対応表を更新してから**ローカルHTMLを再解析**する。推測だけによる新規対応表の自動追加は禁止する。

## 6. 領域別CSVと集計規則

各 `output/areas/<area_id>.csv` はUTF-8（BOMなし）、ヘッダー付き、講演番号順。同一講演は主領域のCSVに1行とし、最低限次の列を持つ：

```csv
presentation_id,registration_id,title,area_id,area_name,joint_areas,format,lecture_type,presenter,presenter_detection,presenter_affiliation_raw,affiliation_alias,affiliation_official,status,source_url
```

複数所属でも講演は1行のままとし、`presenter_affiliation_raw`、`affiliation_alias`、`affiliation_official` の3列には、対応順をそろえたJSON配列をCSVのフィールドとして格納する。未解決の機関は対応する正式名称を `null` とする。集計時はこの配列を展開し、同じ講演・同じ正式機関の組を重複排除する。未解決でも講演行を落とさず、`status` に `resolved`、`unresolved_presenter`、`unresolved_affiliation` 等を記録する。

全体および領域別のランキングは、登壇者の所属にその機関が含まれる**講演の件数**で降順に並べる。1講演に登壇者の所属機関が2つある場合、各機関に1件ずつ加算し、「機関別の件数の合計」は講演総数を超え得る。割合の分母はその集計範囲の対象講演総数とし、「登壇者が当該機関にも所属する講演の割合」と表示する。未解決の講演は分母に含め、どの機関にも恣意的に割り当てない。同数順位は同順位とし、表示順は正式名称の文字列昇順、上位20件を表示する。

## 7. 日本語レポート

`output/report.md` に次を記す。

1. 大会名、対象、取得日、暫定版／最終版、公開プログラムのURL、講演件数ベースの定義。
2. 全体：対象講演数、登壇者判定内訳（明示・筆頭著者推定・単著・未解決）、所属判定済み数と未解決数、機関別上位20位（順位・正式機関名・講演件数・割合）。
3. 領域別：各領域の上記件数と上位20位。合同講演は主領域にのみ帰属する旨を明記する。
4. 注意事項：筆頭著者による推定の件数、複数所属で機関別件数が総講演数を超える可能性、未解決一覧の件数、除外した取消講演件数。未解決が残る場合でも集計上の制約を隠さない。

## 8. 実装順と完了条件

1. 口頭・ポスター・合同・取消・「○」付き／なし・複数所属の実例を少数保存し、HTML構造の読み取りと登壇者規則を確認する。
2. 取得URL一覧と、`wget` を実行するシェルスクリプトまたはGNU Makefileを整備し、それを経由して公開プログラムの必要なHTMLをローカルに保存する。
3. 領域別CSVと未解決一覧を生成し、対応表の根拠を調査して更新する。再解析して集計を作る。
4. 元URLに対する講演番号のサンプル照合、全体件数と主領域別件数の和、重複・取消・未解決の検査、特に筆頭著者推定例の目視確認を行う。失敗・欠落があれば報告書に成功と記さない。
5. 会期後に同じ手順で最終版を作り、変更点と取得日時を報告書に記載する。

完了条件は、対象HTMLの取得漏れがなく、合同講演を全体で重複計上せず、各CSVから報告書の件数が再現でき、未解決ケースと筆頭著者推定の数が明示されていること。`README.md` に実行順序と依存パッケージ、取得用シェルスクリプトまたはGNU Makefileの具体的な実行コマンド、対応表の追加手順を書く。
